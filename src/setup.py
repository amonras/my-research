from setuptools import setup, find_packages

setup(
    name='my-research',
    version='0.0.1',
    description='A tool to summarize a given research body',
    author='Alex Monras',
    license='MIT',
    packages=find_packages('src')
)
